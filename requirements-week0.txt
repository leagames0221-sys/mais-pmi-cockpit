# Week 0 — scaffold + audit phase (doctrine: external-source-audit 通過待ち、 全 ★★★ green flag 予定)
# Week 1+ で heavy deps (apache-superset OR sqlalchemy + scikit-learn + transformers + langgraph + pandas) を追加 audit + install
# CVE-2025-71176 (pytest privilege escalation) fix 維持: pytest>=9.0.3
# T2/T3 学び literal 反映 (doctrine: analogical-recall): pip-audit CI で --disable-pip flag 使用禁止

# Synthetic PMI cockpit data generation (T1/T2/T3 同 stack reuse)
faker>=40.18.0,<41.0
python-dotenv>=1.0,<2.0

# Test infra
pytest>=9.0.3,<10.0
pytest-cov>=5.0,<7.0

# Schema validation
pydantic>=2.5,<3.0

# Audit + drift CI
pip-audit>=2.7,<3.0
